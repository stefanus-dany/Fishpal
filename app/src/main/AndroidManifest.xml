<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.karyaprestasi.fishpal">
    <!-- custom camera -->
    <uses-feature android:name="android.hardware.camera.any" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FishBud">
        <activity
            android:name="com.karyaprestasi.fishpal.ui.recognition.RecognitionActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.karyaprestasi.fishpal.ui.recognition.InfoScanActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.karyaprestasi.fishpal.ui.main_ui.MainActivity" />
        <activity
            android:name="com.karyaprestasi.fishpal.ui.authentication.AuthenticationActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.karyaprestasi.fishpal.SplashScreenActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>